html
    head
        title pub example page
        body
            style.
            //#{} 데이터를 출력할 때 사용
            h1 #{table_name}
            h2 #{number}
            hr
            // - java script 입력할 때
            - for(let i =1; i< 10; i++){
                //p태그 : 단락나누기
                p
                //div 태그명 : multiple
                #multiple
                    #{number} X #{i} = #{number * i} 
            -}
